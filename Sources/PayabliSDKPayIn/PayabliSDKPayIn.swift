import Foundation
import PayabliSDKCore

/// PayabliSDKPayIn — the PayIn component of the Payabli SDK.
///
/// Provides tokenization, card-not-present processing (getpaid), and
/// card-present payments via Tap to Pay on iPhone.
///
/// See RFC-0001 §5 and PRD §5 for the functional requirements.
public enum PayabliSDKPayIn {
    public static let version = "1.0.0"
}
